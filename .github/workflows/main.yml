on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test the action on the file
    steps:
      - uses: actions/checkout@v3
      - uses: thatrandomperson5/AutoMarkdownContents@v0.0.1
        with:
          file: README.md
